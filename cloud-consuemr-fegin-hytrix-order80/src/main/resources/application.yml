server:
  port: 80

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka

  instance:
    instance-id: orderHystrixFegin80
    prefer-ip-address: true

# 开启熔断
feign:
  hystrix:
    enabled: true